/**
 * A docking position around the edge of a chart, named for its compass direction. `"E"` (due
 * right, vertically centered) is the default a `<LegendOverlay>` starts docked at
 */
export type ICompassPosition = "N" | "NE" | "E" | "SE" | "S" | "SW" | "W" | "NW";
