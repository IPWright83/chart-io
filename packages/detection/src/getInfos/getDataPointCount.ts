/**
 * Determine the number of data points in the dataset
 * @param  data     The complete dataset
 * @return          The number of rows
 */
export function getDataPointCount(data: any[] = []) {
    return data.length;
}
