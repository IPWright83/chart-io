/**
 * A completely custom format function, useful for translating to human friendly strings
 * @param name    The key of the series to format
 */
export type ILegendFormatter = (name: string) => string;
