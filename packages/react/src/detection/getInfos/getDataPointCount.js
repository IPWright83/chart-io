/**
 * Determine the number of data points in the dataset
 * @param  {Array<Object>}  data     The complete dataset
 * @return {Number}                  The number of rows
 */
const getDataPointCount = (data) => data.length;

export { getDataPointCount };
